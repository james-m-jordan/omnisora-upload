{
  "name": "omnisora-upload",
  "version": "1.0.0",
  "description": "Frontend build for OmniSora",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "echo 'Use gunicorn to start the Flask backend'"
  }
}