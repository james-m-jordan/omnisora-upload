{
  "name": "omnisora-upload",
  "version": "1.0.0",
  "description": "Unified file upload system with AI tagging and 10TB support",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "python app.py",
    "dev:frontend": "cd frontend && vite",
    "build": "echo 'Build handled by nixpacks.toml'",
    "start": "echo 'Start handled by nixpacks.toml'"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  }
}