{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m venv venv && . venv/bin/activate && pip install -r requirements.txt && npm install && cd frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": ". venv/bin/activate && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 